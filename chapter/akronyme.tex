\newglossaryentry{NFS}{ name={NFS}, description={Network File System (NFS) ist eine verteiltes Dateisystem, welche auf dem Client-Server Protokoll RPC aufbaut. Ein NFS Server Host stellt Clients ein oder mehrere  über NFS zur Verfügung. NFS  

Der NFS Server ist ein Host welche Dateisysteme anderen Host Clients zur Verfügung stellt. }} NFS and NIS


